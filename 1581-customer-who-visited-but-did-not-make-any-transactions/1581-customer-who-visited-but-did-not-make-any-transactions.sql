# Write your MySQL query statement below
select v.customer_id, count(v.customer_id) as count_no_trans from visits v LEFT JOIN transactions t using(visit_id) where t.visit_id is NULL group by customer_id ;